<%= form_tag(admin_dashboard_path, method: "get") do %>
    <%= hidden_field_tag 'clear_news', "true" -%>
  <%= submit_tag("Got it!") %>
  Clicking on this button will silence the news untl the next one is released.
<% end %>