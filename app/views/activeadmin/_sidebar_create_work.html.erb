<ul>
  <%= form_with url: new_admin_work_path, method: :get, local: true do |f| %>

    <%= hidden_field_tag :prototype_source_id, resource.id %>

    <div class="actions">
      <%= submit_tag "Create work" %>
    </div>
  <% end %>
</ul>